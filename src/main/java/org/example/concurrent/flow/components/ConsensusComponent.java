package org.example.concurrent.flow.components;

import org.example.concurrent.flow.events.ConsensusStateEvent;
import org.example.concurrent.flow.events.ConsensusEvent;

public class ConsensusComponent {

    public ConsensusStateEvent getState(ConsensusEvent consusEvent) {
        return null;
    }

}
